spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
api.key=${API_KEY}
jwt.secret=${JWT_SECRET}
encryption.secret.key=${SUBMISSION_HMAC_KEY}
encryption.pii.key=${SUBMISSION_PII_KEY}

# Optional: comma-separated origins for CORS (overrides CorsConfig defaults). Production: https://el-kapitan.vercel.app
# cors.allowed-origins=https://el-kapitan.vercel.app,http://localhost:5173